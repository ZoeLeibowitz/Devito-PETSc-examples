name: Run 1D Poisson & Commit Plots

on:
  push:
    branches:
      - main
    # Ignore commits to PNG files to prevent workflow loops
    paths-ignore:
      - 'Chapter3/3_1_1/*.png'
  schedule:
    # Runs every Monday at 00:00 UTC
    - cron: "0 0 * * 1"
  workflow_dispatch:  # allow manual trigger

jobs:
  run-poisson:
    runs-on: ubuntu-latest

    steps:
    # Checkout repo
    - name: Checkout repo
      uses: actions/checkout@v4

    # Log in to DockerHub
    - name: Log in to DockerHub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    # Pull Docker image
    - name: Pull prebuilt Docker image
      run: docker pull zoeleibowitz/petsc_devito_image:latest

    # Run 3_1_1 - serial
    - name: Run 3_1_1
      run: |
        docker run --rm -t \
          -v ${{ github.workspace }}:/workspace \
          -w /workspace/Chapter3/3_1_1 \
          -u $(id -u):$(id -g) \
          zoeleibowitz/petsc_devito_image:latest \
          python3 1d_poisson.py

    # Run 3_1_1 - parallel
    - name: Run 1d_poisson_mpi.py with MPI
      run: |
        docker run --rm -t \
          -v ${{ github.workspace }}:/workspace \
          -w /workspace/Chapter3/3_1_1 \
          -e DEVITO_MPI=1 \
          -u $(id -u):$(id -g) \
          zoeleibowitz/petsc_devito_image:latest \
          mpiexec -n 4 python3 1d_poisson_mpi.py

    # Configure Git for commits
    - name: Configure git
      run: |
        git config --global user.name "github-actions[bot]"
        git config --global user.email "github-actions[bot]@users.noreply.github.com"

    # Commit and push generated PNGs
    - name: Commit and push plots
      run: |
        git add Chapter3/3_1_1/*.png
        git commit -m "Update plots from workflow" || echo "No changes to commit"
        git push https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }} HEAD:${{ github.ref }}
